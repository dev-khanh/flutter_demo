import 'package:flutter/material.dart';

class badges_widget extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: RaisedButton(
          onPressed: (){},
          child: Text('Click'),
        ),
      ),
    );
  }
}